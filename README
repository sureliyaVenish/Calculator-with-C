The following code is written in Visual Studio Code and it didn't required getch() and clrscr().
And using clrscr(), sometime there were errors in VS Code. So I haven't used those functions here.
But at the same time getch() and clrscr() are required to run programe properly in terminal.
